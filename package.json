{
  "name": "env-sync-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace for the @hardmachinelabs/env-sync package.",
  "scripts": {
    "build": "pnpm --filter @hardmachinelabs/env-sync run build",
    "docs:build": "vitepress build docs",
    "docs:dev": "vitepress dev docs",
    "docs:preview": "vitepress preview docs",
    "typecheck": "pnpm --filter @hardmachinelabs/env-sync run typecheck",
    "test": "pnpm --filter @hardmachinelabs/env-sync run test"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@changesets/cli": "2.31.0",
    "vitepress": "1.6.4"
  }
}
